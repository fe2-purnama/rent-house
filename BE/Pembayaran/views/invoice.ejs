<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Invoice</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 20px;
      }
      .invoice {
        max-width: 800px;
        margin: 0 auto;
        background: #f9f9f9;
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      }
      .invoice-header {
        text-align: center;
        margin-bottom: 20px;
      }
      .invoice-details {
        margin-bottom: 20px;
      }
      .invoice-table {
        width: 100%;
        border-collapse: collapse;
      }
      .invoice-table th,
      .invoice-table td {
        border: 1px solid #ddd;
        padding: 8px;
        text-align: left;
      }
      .invoice-table th {
        background-color: #f2f2f2;
      }
      .invoice-table td {
        border-top: none;
      }
    </style>
  </head>
  <body>
    <div class="invoice">
      <div class="invoice-header">
        <h1>Invoice</h1>
      </div>
      <div class="invoice-details">
        <p>Invoice ID: <strong><%= invoice.invoice_id %></strong></p>
        <p>Customer ID: <strong><%= invoice.customer.user_id %></strong></p>
        <p>Customer Name: <strong><%= invoice.customer.name %></strong></p>
        <p>Customer Email: <strong><%= invoice.customer.email %></strong></p>
        <p>Order Date: <strong><%= invoice.order_date %></strong></p>
      </div>
      <table class="invoice-table">
        <thead>
          <tr>
            <th>Product ID</th>
            <th>Product Name</th>
            <th>Category</th>
            <th>Image URL</th>
            <th>WiFi Access</th>
            <th>Room Count</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><%= invoice.product.product_id %></td>
            <td><%= invoice.product.name %></td>
            <td><%= invoice.product.category %></td>
            <td><%= invoice.product.image_url %></td>
            <td><%= invoice.product.wifi_access %></td>
            <td><%= invoice.product.room_count %></td>
          </tr>
        </tbody>
      </table>
      <div class="invoice-status">
        <p>
          User Role:
          <strong
            ><%= invoice.user_role == 1? 'user' : user_role == 2 ? 'admin' :
            'unknown' %></strong
          >
        </p>
        <p>Order Status: <strong><%= invoice.order_status %></strong></p>
      </div>
    </div>
  </body>
</html>
